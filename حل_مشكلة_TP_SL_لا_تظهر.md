# 🔧 حل مشكلة عدم ظهور TP/SL في الرسائل

## المشكلة:
TP/SL لا تظهر في الرسائل لأن قيمها `null` في JSON.

## السبب:
TradingView لا يستبدل `{{plot("...")}}` لأن أسماء الـ plots في التنبيه لا تطابق أسماء الـ plots في المؤشر.

## الحل:

### 1. **تحقق من أسماء الـ plots في المؤشر:**

في مؤشر TradingView، يجب أن تكون أسماء الـ plots كما يلي:

#### للـ plots الأساسية:
- `plot(tp1, "TP Line 1", ...)` أو `plot(tp1, title="TP Line 1", ...)`
- `plot(tp2, "TP Line 2", ...)` أو `plot(tp2, title="TP Line 2", ...)`
- `plot(tp3, "TP Line 3", ...)` أو `plot(tp3, title="TP Line 3", ...)`
- `plot(sl, "SL Line", ...)` أو `plot(sl, title="SL Line", ...)`

#### للـ Entry Line:
- `plot(entry, "Entry Line", ...)` أو `plot(entry, title="Entry Line", ...)`

### 2. **تحقق من التنبيه في TradingView:**

في إعدادات التنبيه، يجب أن تكون أسماء الـ plots **مطابقة تماماً** للأسماء في المؤشر:

#### ✅ صحيح:
```json
{"signal":"BUY","symbol":"{{ticker}}","entry_price":{{close}},"tp1":{{plot("TP Line 1")}},"tp2":{{plot("TP Line 2")}},"tp3":{{plot("TP Line 3")}},"stop_loss":{{plot("SL Line")}},"time":"{{time}}","timeframe":"{{interval}}"}
```

#### ❌ خطأ (أسماء مختلفة):
```json
{"signal":"BUY","symbol":"{{ticker}}","entry_price":{{close}},"tp1":{{plot("TP1")}},"tp2":{{plot("TP2")}},...}
```

### 3. **كيفية معرفة الأسماء الصحيحة:**

1. افتح المؤشر في TradingView
2. انقر بزر الماوس الأيمن على الخط
3. اختر "Settings" أو "الإعدادات"
4. انظر إلى "Title" أو "Name" - هذا هو الاسم الذي يجب استخدامه

أو:

1. افتح المؤشر في TradingView
2. انظر إلى "Style" في الـ legend
3. الاسم الظاهر هناك هو الذي يجب استخدامه

### 4. **أسماء بديلة شائعة:**

إذا كانت الأسماء في المؤشر مختلفة، جرب هذه الأسماء:

#### بدائل لـ TP:
- `TP Line 1`, `TP1`, `Take Profit 1`, `TP_1`, `TP1 Line`
- `TP Line 2`, `TP2`, `Take Profit 2`, `TP_2`, `TP2 Line`
- `TP Line 3`, `TP3`, `Take Profit 3`, `TP_3`, `TP3 Line`

#### بدائل لـ SL:
- `SL Line`, `Stop Loss`, `StopLoss`, `SL`, `Stop Loss Line`

#### بدائل لـ Entry:
- `Entry Line`, `Entry`, `Entry Price`, `Entry_Price`

### 5. **اختبار التنبيه:**

بعد تعديل التنبيه:
1. احفظ التنبيه
2. انتظر حتى يحدث trigger
3. تحقق من السجلات - يجب أن ترى قيم TP/SL وليس `null`

### 6. **إذا استمرت المشكلة:**

#### الحل البديل - استخدام `{{close}}` أو `{{high}}` / `{{low}}`:

إذا لم تكن الـ plots متاحة، يمكنك استخدام:

```json
{"signal":"BUY","symbol":"{{ticker}}","entry_price":{{close}},"tp1":{{high}},"tp2":{{high}},"tp3":{{high}},"stop_loss":{{low}},"time":"{{time}}","timeframe":"{{interval}}"}
```

**ملاحظة:** هذا لن يعطي TP/SL الصحيحة، لكن سيظهر شيء بدلاً من null.

## ✅ التحقق النهائي:

بعد إصلاح التنبيه، يجب أن ترى في السجلات:
```
INFO:main:📥 Cleaned JSON: {"signal":"BUY","symbol":"BTCUSDT","entry_price":101787.70,"tp1":102000.00,"tp2":102500.00,"tp3":103000.00,"stop_loss":101500.00,...}
```

**وليس:**
```
INFO:main:📥 Cleaned JSON: {"signal":"BUY","symbol":"BTCUSDT","entry_price":101787.70,"tp1":null,"tp2":null,...}
```

## 📝 ملاحظة مهمة:

إذا غيرت المؤشر أو استخدمت مؤشر آخر، **يجب تحديث أسماء الـ plots في جميع التنبيهات** لتطابق الأسماء الجديدة.

