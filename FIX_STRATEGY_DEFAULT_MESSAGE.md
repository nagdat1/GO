# 🔧 حل مشكلة الرسالة الافتراضية من TradingView Strategy

## ⚠️ المشكلة:

TradingView يضيف رسالة افتراضية من `strategy()` حتى عند استخدام `alertcondition()`:

```
nagdat (Trailing, Open/Close, No Filtering, 7, 45, 10, 2, 10, 50, 30, 20, 10): تم تنفيذ الأمر {{strategy.order.action}} @ {{strategy.order.contracts}} على {{ticker}}. المركز الجديدة للإستراتيجية هو {{strategy.position_size}}
```

## ✅ الحل:

### 1. تم تحسين الكود لاستخراج JSON من النص:

الكود الآن يستخرج JSON تلقائياً حتى لو كان مختلطاً مع الرسالة الافتراضية.

### 2. استخدم `alertcondition()` فقط في TradingView Alert:

#### خطوات الإعداد:

1. **افتح TradingView Alert** (الجرس 🔔)

2. **اختر "Webhook URL"** من Alert Type

3. **أدخل الرابط:**
   ```
   https://go-production-e51a.up.railway.app/personal/8169000394/webhook
   ```

4. **⚠️ اترك Message field فارغاً تماماً** ✅

5. **في Condition، اختر من القائمة:**
   - `BUY Signal for Webhook` (وليس "Any alert() function call")
   - `SELL Signal for Webhook`
   - إلخ...

### 3. لماذا Message field يجب أن يكون فارغاً؟

- إذا ملأت Message field: سيستخدم ما كتبته (ولن يستخدم JSON من `alertcondition()`)
- إذا تركته فارغاً: سيستخدم JSON من `alertcondition()` تلقائياً ✅

---

## 🔍 إذا استمرت المشكلة:

### التحقق من Alert Settings:

1. **تأكد أن Condition من `alertcondition()`:**
   - ✅ `BUY Signal for Webhook` (من alertcondition)
   - ❌ `Any alert() function call` (هذا سيضيف الرسالة الافتراضية)

2. **تأكد أن Message field فارغ:**
   - ✅ فارغ تماماً
   - ❌ لا تكتب أي شيء فيه

3. **حذف Alert القديم وإنشاء جديد:**
   - قد يكون Alert القديم يستخدم settings مختلفة
   - احذف Alert القديم وأنشئ جديد

---

## 📝 ملاحظة:

الكود الآن يستخرج JSON تلقائياً حتى لو كان مختلطاً مع الرسالة الافتراضية. لكن الحل الأمثل هو استخدام `alertcondition()` فقط مع Message field فارغ.

---

**بعد تطبيق هذه الخطوات، ستصل الرسائل مع JSON فقط والسعر الحقيقي! 🚀**

