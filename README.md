# 📊 TradingView Webhook to Telegram Bot

مشروع Python بسيط لاستقبال إشارات التداول من TradingView وإرسالها إلى Telegram مع نظام حفظ الصفقات.

## ✨ المميزات

- ✅ استقبال إشارات التداول (BUY, SELL, TP1, TP2, TP3, SL)
- ✅ إرسال رسائل منسقة بالعربية
- ✅ حساب TP/SL تلقائياً
- ✅ حماية من spam و rate limiting
- ✅ **نظام حفظ الصفقات في ملف JSON (يبقى حتى بعد إيقاف المشروع)**
- ✅ واجهات API لاستعراض الصفقات والإحصائيات
- ✅ ملف واحد فقط (`app.py`) - بسيط وسهل

## 🚀 الإعداد السريع

### 1. تثبيت المكتبات
```bash
pip install -r requirements.txt
```

### 2. إعداد المتغيرات
أنشئ ملف `.env` أو اضبط المتغيرات في Railway:
```
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

### 3. تشغيل المشروع
```bash
python app.py
```

أو مع gunicorn:
```bash
gunicorn app:app
```

## 📝 إعداد TradingView

راجع ملف `التنبيهات_البسيطة_8_إشارات.txt` للتعليمات الكاملة.

### مثال JSON للإشارة BUY:
```json
{"signal":"BUY","symbol":"{{ticker}}","entry_price":{{close}},"tp1":{{plot("TP1 Alert")}},"tp2":{{plot("TP2 Alert")}},"tp3":{{plot("TP3 Alert")}},"stop_loss":{{plot("SL Alert")}},"time":"{{time}}","timeframe":"{{interval}}"}
```

### Webhook URL:
```
https://your-domain.com/personal/YOUR_CHAT_ID/webhook
```

## 📊 أنواع الإشارات

- `BUY` / `LONG` - صفقة لونج
- `SELL` / `SHORT` - صفقة شورت  
- `TP1_HIT` / `TP1` - ضرب الهدف الأول
- `TP2_HIT` / `TP2` - ضرب الهدف الثاني
- `TP3_HIT` / `TP3` - ضرب الهدف الثالث 🚀
- `STOP_LOSS` / `SL` - ضرب وقف الخسارة

## 💾 نظام حفظ الصفقات

### الملف:
- `trades.json` - يتم إنشاؤه تلقائياً ويحفظ جميع الصفقات

### البيانات المحفوظة:
- معرف الصفقة (ID)
- الرمز (Symbol)
- نوع الإشارة (Signal)
- سعر الدخول (Entry Price)
- وقت الدخول (Entry Time)
- TP1, TP2, TP3, SL
- الحالة (Status): `open`, `tp1`, `tp2`, `tp3`, `closed`, `sl`
- سعر الخروج (Exit Price)
- وقت الخروج (Exit Time)

### واجهات API:

#### 1. جميع الصفقات:
```
GET /trades
GET /trades?status=open    # الصفقات المفتوحة فقط
GET /trades?status=closed  # الصفقات المغلقة فقط
```

#### 2. صفقات رمز معين:
```
GET /trades/BTCUSDT
```

#### 3. إحصائيات:
```
GET /trades/stats
```

## 🔧 الميزات التقنية

- Rate limiting: 2 ثانية بين الرسائل
- منع التكرار: 60 ثانية للإشارات الرئيسية
- حساب TP/SL تلقائي: إذا لم تكن موجودة في JSON
- معالجة أخطاء: تنظيف JSON من TradingView placeholders
- حفظ دائم: الصفقات محفوظة في `trades.json`

## 📁 الملفات

- `app.py` - الملف الرئيسي (كل شيء في ملف واحد!)
- `trades.json` - ملف حفظ الصفقات (يُنشأ تلقائياً)
- `requirements.txt` - المكتبات المطلوبة
- `Procfile` - للنشر على Railway
- `التنبيهات_البسيطة_8_إشارات.txt` - دليل التنبيهات
- `مؤشر الاتستراتيجية.txt` - كود المؤشر

## ⚠️ ملاحظات مهمة

- تأكد من أن البوت لديه صلاحية "Send Messages" في المجموعة
- أسماء Plots في JSON يجب أن تطابق أسماء Plots في المؤشر
- JSON يجب أن يكون في سطر واحد (minified)
- ملف `trades.json` يُحفظ تلقائياً - يمكنك نسخه كنسخة احتياطية
- على Railway، الملفات المؤقتة قد تُحذف - استخدم قاعدة بيانات للبيانات المهمة

---

**بسيط • سريع • فعال • مع حفظ الصفقات** 🚀
