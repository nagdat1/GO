# 🔧 دليل حل المشاكل - Troubleshooting Guide

## 🐛 المشكلة: الإشارات لا تصل للبوت

### التشخيص السريع ⚡

#### 1️⃣ تحقق من أن المشروع يعمل على Railway

افتح في المتصفح:
- `https://YOUR-RAILWAY-DOMAIN.up.railway.app/`
- يجب أن ترى: `{"service": "TradingView to Telegram Bot", "status": "running"}`

إذا ظهرت رسالة 404:
- المشروع غير مفعّل على Railway
- اذهب إلى Railway Dashboard وتحقق من أن المشروع يعمل

---

#### 2️⃣ تحقق من الرابط الصحيح

افتح في المتصفح:
```
https://YOUR-RAILWAY-DOMAIN.up.railway.app/debug
```

ستحصل على معلومات شاملة:
- ✅ الرابط المكتشف
- ✅ رابط Webhook الصحيح
- ✅ حالة اتصال Telegram
- ✅ جميع متغيرات البيئة

---

#### 3️⃣ تحقق من متغيرات البيئة

في Railway Dashboard:
1. اذهب إلى **Settings** → **Variables**
2. تحقق من وجود:
   - `TELEGRAM_BOT_TOKEN` = `your_bot_token`
   - `TELEGRAM_CHAT_ID` = `your_chat_id`
   - `RAILWAY_PUBLIC_DOMAIN` = `your-app.up.railway.app` (مهم!)

إذا لم يكن `RAILWAY_PUBLIC_DOMAIN` موجوداً:
1. افتح **Settings** → **Domains**
2. انسخ الرابط (مثل: `go-production.up.railway.app`)
3. اضغط **+ New Variable**
4. **Name:** `RAILWAY_PUBLIC_DOMAIN`
5. **Value:** `go-production.up.railway.app` (بدون https://)
6. احفظ
7. اضغط **Redeploy**

---

#### 4️⃣ تحقق من البوت في Telegram

**أهم خطوة!** يجب أن تبدأ محادثة مع البوت أولاً:

1. افتح Telegram
2. ابحث عن: `@nagdat232_bot`
3. اضغط **Start** أو أرسل `/start`
4. تأكد من أن البوت يرد

**بدون هذه الخطوة، لن تصل أي رسائل!**

---

#### 5️⃣ تحقق من الرابط المستخدم في TradingView

يجب أن يكون الرابط بالصيغة التالية:
```
https://YOUR-RAILWAY-DOMAIN.up.railway.app/personal/8169000394/webhook
```

**تحذيرات:**
- ⚠️ لا تضع `http://` (يجب أن يكون `https://`)
- ⚠️ تأكد من أن `8169000394` هو نفس `TELEGRAM_CHAT_ID`
- ⚠️ لا تضيف `/` في نهاية الرابط

---

### 🧪 الاختبارات السريعة

#### اختبار 1: اختبار التيلجرام
افتح في المتصفح:
```
https://YOUR-RAILWAY-DOMAIN.up.railway.app/test-telegram
```

**المتوقع:**
- ✅ `"status": "ok"`
- ✅ رسالة في التيلجرام
- ❌ إذا ظهر خطأ 403، لم تبدأ محادثة مع البوت

---

#### اختبار 2: اختبار Webhook مباشرة
افتح في المتصفح:
```
https://YOUR-RAILWAY-DOMAIN.up.railway.app/test
```

**المتوقع:**
- ✅ `"status": "success"`
- ✅ رسالة اختبار في التيلجرام

---

#### اختبار 3: إرسال إشارة تجريبية
افتح في المتصفح:
```
https://YOUR-RAILWAY-DOMAIN.up.railway.app/send-alert
```

**المتوقع:**
- ✅ `"status": "success"`
- ✅ إشارة تجريبية في التيلجرام

---

### 📊 صفحة التشخيص الكاملة

للحصول على معلومات كاملة:
```
https://YOUR-RAILWAY-DOMAIN.up.railway.app/debug
```

ستحصل على:
- معلومات المشروع
- معلومات Telegram
- جميع متغيرات البيئة
- تعليمات سريعة للإصلاح

---

### 🔄 إعادة النشر

بعد تعديل أي متغير:
1. احفظ في Railway
2. انتظر إعادة النشر التلقائي
3. أو اضغط **Redeploy** يدوياً
4. انتظر حتى يظهر: `✅ Status: Deployed`

---

### 🎯 الحل السريع الشامل

إذا كان كل شيء يعمل ولكن الإشارات لا تصل:

1. **تحقق من TradingView:**
   - تأكد من أن Alert مفعّل
   - تأكد من وضع الرابط الصحيح
   - جرب إعادة حفظ Alert

2. **تحقق من Logs في Railway:**
   - اذهب إلى **Deployments**
   - اضغط على آخر deployment
   - افتح **Logs**
   - ابحث عن:
     - `📥 Processing POST request` (الإشارات الواردة)
     - `✅ Alert sent successfully` (الإشارات المرسلة)
     - `❌ Error` (الأخطاء)

3. **إذا لم تظهر أي logs:**
   - المشكلة في TradingView
   - تأكد من صحة الرابط
   - تحقق من أن Alert فعلاً تم تنفيذه

---

### 📞 الحصول على المساعدة

إذا لم تحل المشكلة، اجمع المعلومات التالية:

1. شاشة من `/debug` endpoint
2. شاشة من Logs في Railway
3. رابط Webhook المستخدم في TradingView
4. نوع Alert في TradingView (Strategy أو Manual)

---

## ✅ قائمة التحقق النهائية

- [ ] المشروع يعمل على Railway
- [ ] جميع متغيرات البيئة محفوظة
- [ ] `RAILWAY_PUBLIC_DOMAIN` محفوظ بشكل صحيح
- [ ] بدأت محادثة مع البوت (`/start`)
- [ ] الرابط في TradingView صحيح
- [ ] Alert مفعّل في TradingView
- [ ] تم اختبار `/test-telegram` ونجح
- [ ] تم اختبار `/send-alert` ونجح
- [ ] Logs في Railway تظهر requests واردة

---

**🎉 إذا أكملت كل الخطوات، الإشارات يجب أن تصل!**

